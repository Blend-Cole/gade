
<div id="customer-wrapper" class="desktop-4 desktop-push-4 mobile-3 pt-3">

<div id="template">
    <div class="template_header">
      <h2 class="title text-center">{{ 'customer.reset_password.title' | t }}</h2>
      <p class="note text-center">{{ 'customer.reset_password.subtext' | t }} {{ email }}</p>
    </div>

    {% form 'reset_customer_password' %}
      {{ form.errors | default_errors }}

      <div id="password" class="clearfix large_form">
        <label class="large" for="password">{{ 'customer.reset_password.password' | t }}</label>
        <input type="password" value="" name="customer[password]" id="customer_password" class="large password" size="16" />
      </div>
      <div id="password_confirm" class="clearfix large_form">
        <label class="large" for="password_confirmation">{{ 'customer.reset_password.password_confirm' | t }}</label>
        <input type="password" value="" name="customer[password_confirmation]" id="customer_password_confirmation" class="large password" size="16" />
      </div>
      <div class="action_bottom">
        <input class="secondary button" type="submit" value="{{ 'customer.reset_password.submit' | t }}" />
      </div>
    {% endform %}
  
  


  
</div>
</div>
